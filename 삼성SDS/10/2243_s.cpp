#include <iostream>

using namespace std;

int N;

int main(void){
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	
	cin >> N;
	int a,b,c;
	for (int i = 0 ; i < N ; i++){
		cin >> a >> b;
		
		if (a == 1){
			
		}
		if (a ==2){
			cin >> c;
			
		}
	}
}

#include <iostream>
#include <algorithm>
#include <cmath>
#include <queue>
#include <vector>
using namespace std;
int int_max = 2147483647;
int k,n;

vector<int> v;
priority_queue <int> q;

void start(0){
	
}



int main(void){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	cin >> n;
	
	v= vector<int> (k);
	for (int = 0; i < k ; i++){
		cin >> v[i];
	}
	
	start(0);
	
}
